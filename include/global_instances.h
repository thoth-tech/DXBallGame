#pragma once

#include "types.h"

// Both instances are defined in program.cpp at the start of the main function

extern GameData game_data;               ///< The global game data.
extern InitialsEntryData initials_entry; ///< The global initials entry data.
